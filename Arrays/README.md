### Four(4)  fundamental operations of Arrays:
 - lookup: O(1)
 - Push or Pop : O(1)
 - Insert: O(n)
 - Delete: O(n)

### Static and Dynamic Arrays
 - Static: Array of fixed size and size do not change over time.
 - Dynamic: Array of variable size and memory is handled by the program it self.